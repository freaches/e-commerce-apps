import React from 'react'
import { ScrollView, Text, View } from 'react-native'

export const Cart = () => {
  return (
    <ScrollView>

    <View>
        <Text>cart</Text>
    </View>
    </ScrollView>
  )
}
